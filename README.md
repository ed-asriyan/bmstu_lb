# Авторизатор bmstu_lb

<h2 align="center" style="font-size: 32px">
   <a href="https://github.com/ed-asriyan/bmstu_lb/releases/tag/v0.1-beta">👉 Скачать тут 👈</a>
</h2>

Программа сама авторизует тебя в бауманской Wi-Fi сети, постоянно провернят наличие соединения с интернетом и, если он пропадает, автоматически логинит заново.

## Как оно работает
При первом запуске программа создает конфиг-файл `bmstu_lb.json` рядом с исполняемым файлом и сразу же завершается. Заполните его своим логином и паролем:
```json
{
   "username": "мой_логин",
   "password": "мой_пароль"
}
```
После чего запустите программу ещё раз.
Если интернет доступен, она будет ждать, пока он пропадёт, затем залогинит компьютер в бауманской сетке, после чего опять ждет, пока пропадёт интернет, и так далее по кругу. Выход осуществляется нажатием на enter.

## Сборка
```bash
go build
```
